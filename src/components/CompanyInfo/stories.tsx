import { Story, Meta } from '@storybook/react'
import CompanyInfo from '.'

export default {
  title: 'CompanyInfo',
  component: CompanyInfo
} as Meta

export const Default: Story = () => <CompanyInfo />
