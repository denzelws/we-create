import { Story, Meta } from '@storybook/react'
import CompanySteps from '.'

export default {
  title: 'CompanySteps',
  component: CompanySteps
} as Meta

export const Default: Story = () => <CompanySteps />
